/* --- A1 SUPER GENIUS OVERRIDE (Add at bottom of adminlte.css) --- */
:root {
    --bs-body-bg: #030303;
    --bs-body-color: #00ff41;
    --lte-sidebar-width: 260px;
}

/* Futuristic Scanline Effect */
body::after {
  content: " ";
  display: block;
  position: fixed;
  top: 0; left: 0; bottom: 0; right: 0;
  background: linear-gradient(rgba(18, 16, 16, 0) 50%, rgba(0, 0, 0, 0.25) 50%), 
              linear-gradient(90deg, rgba(255, 0, 0, 0.06), rgba(0, 255, 0, 0.02), rgba(0, 0, 118, 0.06));
  z-index: 9999;
  pointer-events: none;
  background-size: 100% 4px, 3px 100%;
}

.app-sidebar { border-right: 1px solid #00ff41 !important; }
.card { border: 1px solid #1a1a1a !important; background: #080808 !important; }
.text-success { color: #00ff41 !important; text-shadow: 0 0 5px #00ff41; }
